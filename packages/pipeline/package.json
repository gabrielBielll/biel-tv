{
  "name": "@bieltv/pipeline",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "CLI de ingestão: normaliza → segmenta → cue points → R2 → D1",
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
