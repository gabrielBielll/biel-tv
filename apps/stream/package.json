{
  "name": "@bieltv/stream",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute biel-tv-db --local --file ../../packages/db/migrations/0001_init.sql && wrangler d1 execute biel-tv-db --local --file ../../packages/db/migrations/0002_ingest_jobs.sql"
  },
  "dependencies": {
    "@bieltv/db": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
